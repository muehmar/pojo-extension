buildscript {
    repositories {
        mavenCentral()
    }
}

ext {
    isSnapshot = false
    group = 'io.github.muehmar'
    version = '0.2.4' + (project.ext.isSnapshot ? '-SNAPSHOT' : '')
}

allprojects {
    version = rootProject.ext.version
    group = rootProject.ext.group
}